# 2026-09-11 工作日志

## 文档目录重构与归档（CodeAtlas 需求文档）

**背景**：昨日产出 CodeAtlas 系统图谱梳理工具需求文档 v0.2 及 6 份 JSON Schema 契约，原平铺在 `docs/` 根目录。

**本次动作**：按产品全生命周期建立目录结构并归档。

```
docs/
├── README.md              文档索引 + 归档规范 + 归档决策准则（4 条）
├── 01-需求分析/           需求文档-系统图谱梳理工具.md（从 docs/ 根迁入）
├── 02-设计/schemas/       6 份 JSON Schema 契约（从 docs/schemas/ 迁入）
├── 03-开发/
├── 04-测试/
├── 05-评审/
└── 06-上线复盘/           用户列了 5 个阶段，"等"字由我补为上线复盘（对齐项目六阶段框架）
```

**同步修改**：需求文档内两处 `docs/schemas/` 路径引用 → `docs/02-设计/schemas/`。已 grep 校验无遗留旧路径。

**归档决策准则（已写入 docs/README.md）**：
1. 一份文档只归属一个主阶段（以「产出它的动作」为准，非「谁阅读」）
2. 评审结论不覆盖原文档，独立归档到 05-评审，原文档靠版本号演进
3. 契约随设计走：JSON Schema 归 02-设计，其他阶段只引用不复制
4. 每份主文档头部标版本 + 状态（待评审/已评审/已冻结）+ 负责人

**校验**：6 份 JSON Schema 语法全部通过。

**遗留提醒**：03/04/05/06 为空目录，若后续 git 版本化会丢失，需补 .gitkeep 或首个文件。
