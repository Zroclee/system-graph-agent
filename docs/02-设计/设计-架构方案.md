# CodeAtlas 系统图谱梳理工具 · 架构设计方案

| 项目 | 内容 |
| --- | --- |
| 文档名称 | CodeAtlas 系统图谱梳理工具架构设计方案 |
| 版本 | v1.2（草案，待评审） |
| 状态 | 🔴 待评审 |
| 撰写日期 | 2026-09-18 |
| 最近更新 | 2026-09-23（并入 M0 前置验证结论，见下） |
| 文档负责人 | （待补充） |
| 评审对象 | 架构、前端、后端、测试 |
| 上游文档 | [`docs/01-需求分析/需求文档-系统图谱梳理工具.md`](../01-需求分析/需求文档-系统图谱梳理工具.md) v0.2 |
| 关联交付物 | [`docs/02-设计/schemas/*.json`](./schemas/)（中间产物契约，本设计将其真源迁至 zod） |
| 验证报告 | [`docs/03-开发/M0-前置验证报告-tree-sitter-WASM兼容性.md`](../03-开发/M0-前置验证报告-tree-sitter-WASM兼容性.md) v1.0（✅ 已验证 GO） |

> **一句话定位**：以一个**容器文件夹**（系统根，内部各项目可各自独立 VCS）为工作范围，用**确定性流水线**完成 90% 的代码解构，把不确定性压缩到 **6 个接口化的 LLM 任务**里，产物落 JSON 事实源 + SQLite 派生索引，支持按各项目版本增量刷新，最终投影为六个维度的系统图谱。

### 要点速览（v1.2）

| 要点 | 说明 | 影响章节 |
| --- | --- | --- |
| **运行时定标为 Bun（前置验证已通过）** | §12.2 的 go/no-go 风险已排除：tree-sitter WASM 在 Bun 上 7/7 通过，与 Node 结果逐项一致，**不再编写运行时兼容层** | §12.2 §13 |
| 架构主张确立为「**确定性流水线 + 受控智能**」 | 不做自主 Agent；LLM 是窄接口纯函数，不是会用工具的 Agent | §2 |
| **monorepo 工程结构**与包依赖规则 | 10 个包，依赖倒置，边界由 dependency-cruiser 强制 | §3 |
| **系统根 = 容器文件夹** | 内部各项目可各自独立 VCS 仓；`.codeatlas/` 为旁挂产物目录 | §2.4 |
| **zod 为 Schema 单一真源** | 取代手写 JSON Schema，自动导出，消灭类型漂移 | §4.1 |
| **节点 ID 内容寻址规范** | 不含行号、不含自增，保证增量可对齐 | §4.4 |
| **S0–S7 阶段契约** | 每阶段的输入、输出、幂等性、重跑粒度、失败模式 | §5 |
| **6 个 LLM 任务规格** | 每个都有触发条件、结构化输出、必填 fallback | §6 |
| **LLM 编排自研，不接 LangChain** | 与本项目「确定性流水线」主张冲突，能力错配、依赖漂移风险高 | §6.1 §12 |
| **Provider 与密钥边界** | 本地直连 API；Key 只存在于本机进程，绝不下发浏览器 | §6.5 |
| **四道闸门 + 证据回验** | 打开源文件验证片段真实存在，反幻觉的硬机制 | §7 |
| **JSON 为事实源、SQLite 为派生索引** | 两者职责分离，索引可随时删除重建；**不内置产物版本管理** | §8.1 |
| **增量失效算法** | 含跨项目契约扩散；缓存键必须含「依赖契约面哈希」 | §9 |
| **增量默认保守升级** | 拿不准即全量，宁可慢一次不可错一版 | §9.4 |

---

## 目录

- [1. 设计目标与不变量](#1-设计目标与不变量)
- [2. 总体架构](#2-总体架构)
- [3. Monorepo 工程结构](#3-monorepo-工程结构)
- [4. 领域模型与 Schema 策略](#4-领域模型与-schema-策略)
- [5. 阶段契约（S0–S7）](#5-阶段契约s0s7)
- [6. LLM 任务规格](#6-llm-任务规格)
- [7. 四道闸门与证据回验](#7-四道闸门与证据回验)
- [8. 存储设计](#8-存储设计)
- [9. 增量失效算法](#9-增量失效算法)
- [10. 范围守卫设计](#10-范围守卫设计)
- [11. 错误处理与断点续跑](#11-错误处理与断点续跑)
- [12. 技术选型](#12-技术选型)
- [13. 落地路线](#13-落地路线)
- [14. 风险与决策记录](#14-风险与决策记录)

---

## 1. 设计目标与不变量

### 1.1 设计目标

| # | 目标 | 度量方式 |
| --- | --- | --- |
| G1 | 同一份代码产出同一张图 | 同一 commit 连续两次全量运行，`graph.json` 字节级一致（除时间戳） |
| G2 | 增量结果与全量语义等价 | 增量跑完后与全量跑完的 `graph.json` 节点/边集合完全一致 |
| G3 | 每条边可溯源 | 100% 的边携带证据（文件 + 行号 + 片段），且片段可回验通过 |
| G4 | 上下文不丢失 | 任意时刻 kill 进程，重启后可从已完成模块继续，无重复劳动 |
| G5 | 智能可归因 | 每个 LLM 任务可独立开关，开关前后的召回率/误报率可对比 |
| G6 | 技术栈可扩展 | 新增一个技术栈只增加声明式模板，不改核心包代码 |

### 1.2 不变量（必须始终成立，违反即为缺陷）

| # | 不变量 | 违反后果 | 保障机制 |
| --- | --- | --- | --- |
| I1 | 所有文件访问经过 `ScopedFs` | 图谱被系统外代码污染 | §10 单一出口 + 架构测试 |
| I2 | 每个节点/边的 `source.file` 必落在白名单内 | 同上 | §5 S5 装配时校验，越界即剔除并记缺陷 |
| I3 | LLM 不直接写任何产物 | 幻觉进入事实源 | §7 propose → validate → commit |
| I4 | 每条边有证据且证据可回验 | 图谱不可信、无法定位问题 | §7 闸门二 |
| I5 | 节点 ID 由内容推导，不含行号/自增 | 增量 diff 全面失效 | §4.4 |
| I6 | SQLite 可从 JSON 事实源完全重建 | 索引损坏后数据不可恢复 | §8.3 重建一致性测试 |
| I7 | 模块缓存键包含其依赖契约面的哈希 | 后端改了 API 签名但前端模块命中缓存，漏重校验 | §9.3 |
| I8 | 增量推导拿不准即升级为全量 | 静默漏报，图谱悄悄过期 | §9.4 |
| I9 | **一切文件路径的基准是系统根（容器根）**，而非任何子项目根或 VCS 仓根 | 两个子项目下的同名文件互相覆盖；节点 ID 内容寻址失效，增量无法对齐 | §2.4 §4.4 §8.4 |

### 1.3 明确不做的事

- ❌ 不做运行时链路追踪（不接 APM / 埋点），只做**静态代码解构**
- ❌ 不做跨系统的全局图谱（边界由 `system.json` 硬性圈定，跨系统只建 `external` 节点）
- ❌ 不做代码修改或重构建议
- ❌ 不让 LLM 自由决定"下一步读哪个文件"（流水线顺序固定）
- ❌ **不内置产物的版本管理** —— VCS 在本设计中只作为**输入侧的变更传感器**（读被分析系统的提交历史，推导要重跑哪些模块）；对 `.codeatlas/` 产物自身做版本化不在工具职责内，由部署侧自行决定（详见 §8.1）

> **为什么明确划掉第三条**：这两件事都用到 Git，但方向相反——一个在流水线**入口**读别人的历史，一个在**出口**写自己的历史。混在一起会让工具承担不该承担的仓库管理职责（初始化仓库、生成提交、处理冲突），并让"产物目录"的定位变得含混。v1.1 只保留入口侧。

---

## 2. 总体架构

### 2.1 核心主张

> **代码能"证明"的，绝不给 LLM；代码只能"猜"的，交给 LLM 但限定在候选集内；两者都不行的交给人。**

这是全套设计的第一性原理，后续所有结构都是它的推论。

**为什么不做自主 Agent（ReAct / 工具循环）**：

| 维度 | 自主 Agent | 确定性流水线（本设计） |
| --- | --- | --- |
| 可复现 | 同一输入可能产出不同结果 | 除 LLM 任务外逐字节可复现 |
| 可增量 | 无法界定"重跑什么" | 阶段边界即失效最小单位 |
| 可验证 | 只能看最终结果对不对 | 每阶段产物可单独断言 |
| 成本 | 不可预估 | 设计期即可算出调用上限 |
| 调试 | 出错难归因 | 缺陷可定位到阶段、文件、行号 |

**Agent 性被保留在哪**：只有 S3 链路梳理内部，存在"下一个读哪个文件"的开放性，但也仅限在模块范围内的受控探索。其余全部是固定顺序。

### 2.2 分层

```
┌─ 入口层 ───────────────────────────────────────────┐
│  @codeatlas/cli            @codeatlas/web          │
└────────────────────┬───────────────────────────────┘
                     │
┌─ 编排层 ───────────▼───────────────────────────────┐
│  @codeatlas/pipeline                               │
│  StageRunner · 阶段调度 · 断点续跑 · --dry-run      │
│  · 审计留痕 · 缺陷收集                              │
└────────────────────┬───────────────────────────────┘
                     │
┌─ 阶段层（顺序固定）─▼───────────────────────────────┐
│  S0 圈定边界 → S1 技术判定 → S2 项目解构            │
│  → S3 链路梳理 → S4 人工确认 → S5 图谱装配          │
│  → S6 维度投影 → S7 渲染输出                        │
└────────────────────┬───────────────────────────────┘
                     │
┌─ 能力层（可插拔，唯一容纳智能）─────────────────────┐
│  @codeatlas/tech    技术栈模板（声明式）+ 判定器    │
│  @codeatlas/extract 提取器链：AST → 正则 → SQL      │
│  @codeatlas/llm     6 个 LlmTask + Provider 适配    │
│  （图谱投影器注册表：六个 Projector）                │
└────────────────────┬───────────────────────────────┘
                     │
┌─ 领域与基础层 ─────▼───────────────────────────────┐
│  @codeatlas/core   领域模型 · zod Schema · 图算法    │
│  @codeatlas/scope  ScopedFs 范围守卫（单一出口）     │
│  @codeatlas/store  JSON 事实源 · SQLite 索引 · 快照  │
│  @codeatlas/git    VCS 适配 · changeset 推导         │
└────────────────────────────────────────────────────┘
```

### 2.3 数据流

```
源文件
  │  ①  ScopedFs 读取（唯一入口，白名单校验 + 审计）
  ▼
文件内容 + AST
  │  ②  提取器（纯函数：接收内容，不读文件）
  ▼
候选事实（API / 数据访问 / 组件 / 导入关系）
  │  ③  四道闸门（schema · 证据回验 · 置信度 · 冲突消解）
  ▼
事实源 JSON（.codeatlas/facts/**.json）
  │  ④  索引
  ▼
SQLite（可随时删除重建）
  │  ⑤  装配 + 冲突消解
  ▼
graph.json（节点 + 边）
  │  ⑥  六个 Projector 投影
  ▼
dimensions/d1..d6.json
  │  ⑦  渲染
  ▼
CLI 文本 / Mermaid / DOT  ·  Web 画布
```

关键：**LLM 只出现在 ② 的未命中分支**，且其产物同样要过 ③ 才能成为事实。LLM 不参与 ④⑤⑥⑦。

---

### 2.4 系统根与三个实体的定义

本工具有三个容易混淆的"根"，必须先钉死。

| 实体 | 是什么 | 谁提供 | 典型形态 |
| --- | --- | --- | --- |
| **工具仓库** | 本工具自身的代码仓（CLI / Web / 各核心包） | 我们开发 | 一个 Git 仓 |
| **系统根（容器文件夹）** | 装着被分析系统**全部项目**的**一个文件夹** | 使用者提供 | 一个普通目录，**内部各项目可各自是独立 VCS 仓** |
| **`.codeatlas/`** | 工具在系统根下落的**产物目录**（事实源、图谱、快照、索引） | 工具生成 | 系统根下的一个隐藏目录，**旁挂**，不参与被解构 |

```
<系统根>/                        ← 工作范围由它圈定（§10 范围守卫）
├── frontend/                    可能是一个 Git 仓，commit = a1b2c3
├── backend/                     可能是另一个 Git 仓，commit = d4e5f6
├── shared-lib/                  可能没有版本管理
└── .codeatlas/                  ← 产物目录，与被分析项目平级
    ├── system.json
    ├── facts/
    ├── graph/
    ├── snapshots/
    └── index.db
```

三条由上面这个形态**直接推出**的硬约束：

1. **路径基准统一为系统根**（不变量 I9）。节点 ID、证据、变更集、模块归属里的所有文件路径，一律相对**容器根**书写。若各子仓用自己的仓库根做基准，`frontend/src/index.ts` 与 `backend/src/index.ts` 会互相覆盖，内容寻址也会失效。
2. **每个模块归属唯一一个版本源**——判定规则为「**最近 VCS 根**」：从模块目录向上回溯，找到最近的、包含它的版本控制根。保证不会出现同一文件被两个仓认领。
3. **版本基线是一个多仓向量，而不是单个提交号**。因为系统根下可能有 N 个互不相关的仓，`Baseline` 必须逐项目记录其版本锚点，并对无版本管理的项目走内容哈希降级（见 §9.1、`system.schema.json` 的 `versionBaseline.projects[]`）。

> **术语收敛**：v0.2 需求文档中曾出现的「工作空间」一词已统一为「**系统** + **系统根**」。"工作空间"不再作为独立概念使用，避免与"系统根是一个文件夹"这件事混淆。

---

## 3. Monorepo 工程结构

### 3.1 包划分

采用 **Bun workspaces** + TypeScript project references。

> **取舍说明**：Bun workspaces 的依赖隔离严格程度弱于 pnpm（对幽灵依赖更宽容）。本项目的依赖规则主要靠 §3.2 的矩阵 + §3.3 的 dependency-cruiser 静态校验兜底，因此可以接受这一差异，换来**运行时与工具链统一为 Bun**（TS 直跑、内置 SQLite / 测试 / 打包）的简洁性。若后续发现幽灵依赖造成实际问题，可**仅把包管理退回 `pnpm install`**，不影响运行时与其余设计。

```
codeatlas/
├── package.json                 # workspace root
├── pnpm-workspace.yaml
├── tsconfig.base.json
├── .dependency-cruiser.cjs      # 架构边界规则
├── packages/
│   ├── core/        @codeatlas/core
│   ├── scope/       @codeatlas/scope
│   ├── store/       @codeatlas/store
│   ├── git/         @codeatlas/git
│   ├── tech/        @codeatlas/tech
│   ├── extract/     @codeatlas/extract
│   ├── llm/         @codeatlas/llm
│   ├── pipeline/    @codeatlas/pipeline
│   ├── cli/         @codeatlas/cli
│   └── web/         @codeatlas/web
└── docs/                        # 本文档所在
```

| 包 | 职责 | 关键导出 |
| --- | --- | --- |
| `core` | 领域模型、zod Schema、图算法、ID 生成、纯工具函数 | `Node` `Edge` `Evidence` `Confidence` `Module` `TechTemplate`（**类型**） |
| `scope` | 白名单解析、`ScopedFs`、越界审计 | `ScopedFs` `Whitelist` |
| `store` | JSON 事实源读写、SQLite schema 与查询、内容寻址、快照、断点续跑 | `FactStore` `IndexStore` `SnapshotStore` |
| `git` | **VCS 适配**（Git 为首个实现）、多仓基线采集、changeset 推导 | `VcsAdapter` `GitAdapter` `BaselineCollector` `deriveChangeset()` |
| `tech` | 技术栈模板（**数据**）、判定器、模板注册表 | `templates/*` `detectStack()` `TemplateRegistry` |
| `extract` | 提取器实现、提取器注册表 | `Extractor` `extractors/*` |
| `llm` | `LlmTask` 接口、6 个任务实现、Provider 适配、调用留痕 | `LlmTask` `registry` `OpenAiCompatProvider` |
| `pipeline` | Stage 接口、Runner、S0–S7 实现、6 个 Projector | `Stage` `StageRunner` `stages/*` `projectors/*` |
| `cli` | 命令行入口 | `bin/codeatlas` |
| `web` | Web 服务与可视化画布 | — |

### 3.2 依赖规则矩阵

行 = 允许依赖的包，列 = 被依赖的包。

| ↓依赖者 \ 被依赖→ | core | scope | store | git | tech | extract | llm | pipeline |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **core** | — | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| **scope** | ✓ | — | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| **store** | ✓ | ✗ | — | ✗ | ✗ | ✗ | ✗ | ✗ |
| **git** | ✓ | ✗ | ✗ | — | ✗ | ✗ | ✗ | ✗ |
| **tech** | ✓ | ✗ | ✗ | ✗ | — | ✗ | ✗ | ✗ |
| **extract** | ✓ | ✗ | ✗ | ✗ | ✗ | — | ✗ | ✗ |
| **llm** | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | — | ✗ |
| **pipeline** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| **cli** | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ | ✓ |
| **web** | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ | ✓ |

三条硬规则：

1. **`core` 是唯一的最内层**，不允许依赖任何 `@codeatlas/*`（只允许 `zod`）。
2. **中间层（scope / store / git / tech / extract / llm）互不依赖**。这是依赖倒置的落点：提取器需要技术栈模板，但 `extract` 不 import `tech`——模板的**类型**定义在 `core`，模板的**实例**由 `pipeline` 注入给提取器。
3. **`pipeline` 是唯一汇聚点**，`cli` / `web` 只能经由它访问中间层。

### 3.3 边界强制手段

规则不靠自觉，靠 CI 卡：

```js
// .dependency-cruiser.cjs 核心规则摘录
{
  name: 'core-must-be-leaf',
  severity: 'error',
  from: { path: '^packages/core/src' },
  to:   { path: '^packages/(?!core)' }
},
{
  name: 'no-cross-middleware',
  severity: 'error',
  from: { path: '^packages/(scope|store|git|tech|extract|llm)/src' },
  to:   { path: '^packages/(?!core)' }
},
{
  name: 'no-raw-fs-outside-io-packages',
  severity: 'error',
  from: { path: '^packages/(core|tech|extract|llm|pipeline)/src' },
  to:   { dependencyTypes: ['core'], path: '^(node:)?fs$' }
},
{
  name: 'only-io-packages-may-write',
  severity: 'error',
  from: { path: '^packages/(core|tech|extract|llm|git)/src' },
  to:   { path: '^(node:)?fs/promises$' }
}
```

`no-raw-fs-outside-io-packages` 是不变量 I1 的编译期保障：**阶段代码物理上无法直接读文件**，只能通过注入的 `ScopedFs`。

---

## 4. 领域模型与 Schema 策略

### 4.1 zod 为单一真源

**决策**：废弃手写 JSON Schema，改为 **zod 定义 → 自动导出 JSON Schema**。

```
packages/core/src/schema/
  ├── evidence.ts      → 导出 evidence.schema.json
  ├── module.ts        → 导出 module-index.schema.json
  ├── tech-profile.ts  → 导出 tech-profile.schema.json
  ├── graph.ts         → 导出 graph.schema.json
  ├── link.ts          → 导出 link.schema.json
  ├── system.ts        → 导出 system.schema.json
  └── changeset.ts     → 导出 changeset.schema.json
```

| 原因 | 说明 |
| --- | --- |
| 消灭类型漂移 | 手写 schema 与 TS 类型是两份真源，必然分叉 |
| 运行时与编译期同源 | 同一份 zod 既产出 TS 类型，又用于闸门一的运行时校验 |
| 文档自动同步 | `pnpm run schema:export` 重新导出，评审文档永远与代码一致 |

导出脚本产出到 `docs/02-设计/schemas/`，即上游文档引用位置保持不变。

> **迁移动作**：现有 6 份手写 schema 转为"由 zod 导出的生成物"，并在此目录加 `README` 标注"本目录文件自动生成，请勿手工编辑"。

### 4.2 核心实体

```ts
// ---- 证据 ----
interface Evidence {
  file: string;          // 相对系统根路径，必须落在白名单内
  line: number;          // 起始行号（1-based）
  snippet: string;       // 代码片段（原始文本，允许 LLM 被验证的锚点）
  extractorId: string;   // 产出该证据的提取器标识，如 'java.spring.route'
}

// ---- 置信度 ----
type Confidence = 'certain' | 'probable' | 'speculative';

// ---- 节点 ----
interface GraphNode {
  id: string;                    // 见 §4.4
  dimension: DimensionId;
  nodeType: NodeType;
  name: string;
  attrs: Record<string, unknown>;
  source: Evidence;              // 不变量 I2：装配时校验 file ∈ 白名单
}

// ---- 边 ----
interface GraphEdge {
  id: string;
  dimension: DimensionId;
  src: string;                   // NodeId
  dst: string;                   // NodeId
  edgeType: EdgeType;
  confidence: Confidence;
  evidence: Evidence[];          // 至少 1 条，闸门二强制
  producedBy: string;            // extractorId 或 llmTaskId —— 归因的关键字段
}

// ---- 事实（提取器输出，尚未过闸门）----
interface Candidate<T> {
  value: T;
  confidence: Confidence;
  evidence: Evidence[];
  producedBy: string;
}
```

`producedBy` 字段贯穿全程：它让"这条边是 LLM 猜的还是代码证的"成为可查询的数据，直接支撑 G5（智能可归因）。

### 4.3 六个图谱维度

维度不是六张独立的图，而是**同一份事实的六种投影**，定义写在 `core`：

```ts
interface DimensionDef {
  id: DimensionId;                  // 'd1'..'d6'
  name: string;
  nodeTypes: NodeType[];            // 节点类型白名单
  edgeTypes: EdgeType[];            // 边类型白名单
  layout: 'hierarchical' | 'force' | 'dagre';
  collapseBy?: NodeType;            // 默认折叠层级
}
```

| 维度 | 名称 | 节点类型 | 边类型 | 布局 |
| --- | --- | --- | --- | --- |
| D1 | 项目架构 | project, module | depends-on, contains | hierarchical |
| D2 | 前后端调用 | component, api | calls, renders | dagre |
| D3 | 数据访问 | module, table | reads, writes, fk | dagre |
| D4 | 端到端链路 | component, api, service, table | calls, invokes, reads, writes | dagre |
| D5 | 数据模型 | table, column | fk, pk-ref | force |
| D6 | 模块依赖 | module, external | depends-on, leaves-system | hierarchical |

---

### 4.4 节点 ID 规范

ID 格式：

```
NodeId := <nodeType> ":" <scope> ( ":" <discriminator> )*
```

| 节点类型 | ID 格式 | 示例 |
| --- | --- | --- |
| project | `project:<name>` | `project:backend` |
| module | `module:<project>:<moduleRelPath>` | `module:backend:src/main/java/com/acme/order` |
| service | `service:<project>:<fqSymbol>` | `service:backend:com.acme.order.OrderService` |
| api | `api:<project>:<METHOD>:<normalizedPath>` | `api:backend:GET:/api/v1/orders` |
| component | `component:<project>:<fileRelPath>` | `component:web:src/views/OrderList.vue` |
| route | `route:<project>:<path>` | `route:web:/order/list` |
| table | `table:<schema>:<tableName>` | `table:main:t_order` |
| column | `column:<schema>:<tableName>.<columnName>` | `column:main:t_order.id` |
| external | `external:<kind>:<identifier>` | `external:api:GET:/thirdparty/pay` |

> **路径基准说明（不变量 I9）**：表中 `<project>` 与其后的 `<...RelPath>` **直接拼接**，即构成**相对系统根**的完整路径。例如 `component:web:src/views/OrderList.vue` 对应系统根下的 `web/src/views/OrderList.vue`。`<project>` 由该项目在系统根下的目录路径派生（§2.4），因此 **ID 中不存在任何以子仓根为基准的片段**——即便系统根下同时存在 `frontend/src/index.ts` 与 `backend/src/index.ts`，两者的 ID 也天然不同。

**四条禁止**（直接对应不变量 I5）：

1. ❌ **禁止包含行号** —— 行号一动 ID 就变，全图 diff 爆炸，增量彻底失效
2. ❌ **禁止自增序号或随机 UUID** —— 无法与上一版对齐，无法判断"这是不是上次那个节点"
3. ❌ **禁止包含绝对路径** —— 换机器、换检出目录即失效；一律用相对系统根的路径
4. ❌ **禁止包含易变内容**（内容哈希、时间戳）—— 内容一改 ID 就变，增量会误判成"删节点 + 加节点"

#### 4.4.1 路径规范化算法（ID 生成与契约匹配共用）

```
/api/v1/orders/{orderId}   ─┐
/api/v1/orders/${orderId}   │
/api/v1/orders/:orderId     ├──►  /api/v1/orders/{p}
/api/v1/orders/[orderId]    │
/api/v1/orders/{id}/items   ─┘     /api/v1/orders/{p}/items
```

步骤：

1. 去掉末尾斜杠
2. 折叠重复斜杠
3. 按 `system.json` 声明的 `stripPrefixes` 剥离网关前缀（如 `/gateway`、`/api-gateway`）
4. 所有路径参数段统一替换为 `{p}`（覆盖 `{x}` `${x}` `:x` `[x]` 四种写法）
5. 查询串按白名单保留参与路由的参数，其余丢弃
6. 字面量数字段不归一化（`/orders/12345` 保持原样，它通常不是路由声明）

**为什么规范化必须进 ID**：如果 ID 用原始路径，`/orders/{id}` 和 `/orders/${id}` 会生成两个节点，前后端永远匹配不上。规范化之后，跨项目契约匹配从"模糊匹配问题"降级为"**ID 相等判断**"——这是把原本可能交给 LLM 的活儿抢回代码手里的关键一手。只有规范化后仍不等的（如 `/order` vs `/orders`）才落到 L1 语义对齐。

---

## 5. 阶段契约（S0–S7）

### 5.1 阶段总览

| 阶段 | 输入 | 输出 | 执行者 | 重跑粒度 | 幂等 | 含 LLM |
| --- | --- | --- | --- | --- | --- | --- |
| **S0** 圈定边界 | `system.json` | `whitelist.json` `audit` | 代码 | 整系统 | ✓ | — |
| **S1** 技术判定 | 各项目根 | `tech-profile.json` | 代码 | 单项目 | ✓ | L6 |
| **S2** 项目解构 | `tech-profile` | `module-index.json` | 代码 | 单项目 | ✓ | L4 |
| **S3** 链路梳理 | `module-index` `tech-profile` | `link/<moduleId>.json` `confirm/<moduleId>.md` | 代码 | **单模块** | ✓ | L1 L2 L3 |
| **S4** 人工确认 | `link/*.json` | 状态流转 `pending→confirmed` | 代码 + **人** | 单模块 | ✓ | L5 |
| **S5** 图谱装配 | confirmed links | `graph.json` | 代码 | 全量 | ✓ | — |
| **S6** 维度投影 | `graph.json` | `dimensions/d1..d6.json` | 代码 | 全量 | ✓ | — |
| **S7** 渲染输出 | `dimensions/*` | 文本 / Mermaid / DOT / 画布 | 代码 | 全量 | ✓ | — |

**流水线约定**：`Stage<In, Out>` 统一接口，接受 `PipelineContext`（含 `systemId`、`scopedFs`、`factStore`、`indexStore`、`logger`、`llmRegistry`、`dryRun`）。断点续跑、审计、缺陷收集、`--dry-run` 全部在 `StageRunner` 里实现一次，阶段实现者不感知。

```ts
interface Stage<In, Out> {
  readonly id: 'S0' | 'S1' | 'S2' | 'S3' | 'S4' | 'S5' | 'S6' | 'S7';
  readonly rerunGranularity: 'system' | 'project' | 'module' | 'full';
  /** 是否可跳过（依据指纹）；S5–S7 恒返回 false */
  canSkip(ctx: PipelineContext, input: In): Promise<boolean>;
  run(ctx: PipelineContext, input: In): Promise<Out>;
}
```

### 5.2 S0 圈定边界

| 项 | 内容 |
| --- | --- |
| 输入 | `system.json`（用户声明：系统名、项目白名单、`stripPrefixes`；`rootDir` 通常省略） |
| 输出 | 规范化白名单（含 `realpath` 解析结果）、初始审计记录 |
| 失败模式 | 白名单为空 → **立即中止**，不进入 S1；路径不存在 → 记为缺陷并剔除该项目 |
| 关键动作 | 推定系统根（默认取 `.codeatlas/` 的上一级），对每个白名单根做 `realpath`，落库真实路径；建立 `ScopedFs` 单例 |

> **符号链接陷阱**：白名单校验必须在 `realpath` 之后进行。否则可以通过一个指向系统外的软链绕过守卫。本项目自身不再跟随额外软链层级。

### 5.3 S1 技术判定

| 项 | 内容 |
| --- | --- |
| 输入 | 各项目根目录 |
| 输出 | `tech-profile.json`：每项目的栈、框架、版本、入口约定、模板 id |
| 主要手段 | 读 manifest：`package.json` / `pom.xml` / `build.gradle` / `requirements.txt` / `pyproject.toml` / `go.mod` |
| 判定依据 | 依赖特征（如 `vue@3` + `vite` → `vue3-vite`）+ 配置特征（`vue.config.js` vs `vite.config.ts`）+ 文件特征（`.vue` 存在） |
| 失败模式 | manifest 缺失或信号矛盾 → 触发 **L6**；L6 也失败 → 标记 `unknown`，**不中止**，但 S2/S3 对该项目降级为"整项目单模块 + 仅正则提取" |

### 5.4 S2 项目解构

| 项 | 内容 |
| --- | --- |
| 输入 | `tech-profile.json` |
| 输出 | `module-index.json`：模块清单（id、相对路径、类型、构建单元归属） |
| 模块划分依据 | 优先级：① 构建单元（Maven module / workspace package）② 模板声明的模块约定（如 Spring 的 `@Service` 包根、Vue 的 `views/` 目录）③ 目录层级 + 文件数阈值 |
| 执行者 | 代码划分 + **L4** 生成业务摘要与标签（可降级为目录名） |
| 失败模式 | 模块划分失败 → 退化为"整个项目 = 一个模块"，仍可继续 |

> **「模块」是整个系统的原子单元**：它是 S3 的重跑粒度、S4 的确认粒度、断点续跑的检查点粒度。模块划分粒度直接决定增量效率——太粗则每次重跑过重，太细则确认负担过重。默认建议维持在**业务域级别**（如 `order`、`user`、`payment`），而非包级别。

### 5.5 S3 链路梳理（核心阶段）

S3 按模块逐个执行，模块内部分六个子步骤：

| 子步骤 | 动作 | 执行者 |
| --- | --- | --- |
| 3.1 API 提取 | 扫路由注解/装饰器/函数调用 → API 清单；类级前缀 + 方法级路径拼接；签名与参数 | 代码（+ **L2** 动态路由） |
| 3.2 数据访问提取 | ORM 实体 ↔ 表映射；Repository/Mapper 静态 SQL；裸 SQL 表名抽取 | 代码（+ **L3** 动态 SQL） |
| 3.3 前端调用提取 | HTTP 客户端调用点；组件 → store → api 模块 → HTTP 调用链；路由 ↔ 页面映射 | 代码 |
| 3.4 契约匹配 | 前端调用 URL ↔ 后端 API：**先 ID 相等，再规范化相等，最后才 L1 语义对齐** | 代码 → **L1** |
| 3.5 模块依赖 | 后端 import / 依赖注入图；跨模块 service 调用 | 代码 |
| 3.6 证据与置信度 | 记录 `文件:行号 + 片段`；按提取器来源打分；初步去重 | 代码 |

**输出**（每个模块两份）：

- `link/<moduleId>.json` —— 结构化事实，供 S5 消费
- `confirm/<moduleId>.md` —— 人类可读的确认文档，供 S4 评审（含 **L5** 生成的链路业务说明）

**为什么同时产出 JSON 和 Markdown**：JSON 给机器，Markdown 给人。人工确认的输入必须是可读的，否则确认动作会退化成"无脑点通过"。这也正是需求里"在文档确认 JSON 对应节点信息、业务逻辑"的落点。

### 5.6 S4 人工确认

| 项 | 内容 |
| --- | --- |
| 输入 | `confirm/<moduleId>.md` + `link/<moduleId>.json` |
| 输出 | 模块状态：`pending → confirmed` / `rejected` / `corrected` |
| 执行者 | **人**（这是唯一的强人工闸门） |
| 规则 | **S5 只消费 `confirmed` 与 `corrected` 的模块**；`pending` 模块以"低可信"标记进入图谱并在报告中标红，`rejected` 模块整体排除 |
| 失败模式 | 全部 `pending` → 图谱仍可产出，但报告显著标注"未经人工确认" |

### 5.7 S5 图谱装配

| 项 | 内容 |
| --- | --- |
| 输入 | 所有 confirmed / corrected 的 `link/*.json` |
| 输出 | `graph.json`（nodes + edges + dimensions 定义） |
| 动作 | ① 节点去重（按 ID）② 边合并（同 `src+dst+type` 合并且证据取并集）③ 冲突消解（§7 闸门四）④ **白名单校验**（不变量 I2，越界即剔除并记缺陷）⑤ 孤岛节点与悬挂边检测 |
| 失败模式 | 校验不过的节点被剔除，`graph.json` 仍产出，缺陷汇总进报告 |

> **悬挂边**是重要信号：某条边的 `src` 或 `dst` 节点不存在，通常意味着"前端调了一个后端没实现的接口"（或反之，漏识别）。这类边不静默删除，而是以 `dangling: true` 保留，单独成一张风险清单。

### 5.8 S6 维度投影

六个 `Projector` 从同一份 `graph.json` 投影。`Projector` 是纯函数，`(graph, dimDef) => DimensionGraph`。

| 投影动作 | 说明 |
| --- | --- |
| 过滤 | 按 `dimDef.nodeTypes` / `edgeTypes` 白名单裁剪 |
| 折叠 | 按 `dimDef.collapseBy` 把子节点折叠到父节点（如 D1 把 api 折进 module） |
| 聚合 | 折叠后合并重复边，权重累加 |
| 布局参数 | 带上 `layout` 供渲染层使用 |

### 5.9 S7 渲染输出

| 通道 | 形态 |
| --- | --- |
| CLI | 文本树、Mermaid、DOT、CSV；`--dimension d4` 切换维度 |
| Web | 服务端读 `dimensions/*.json`，前端画布渲染（**AntV G6**），支持维度切换、层级折叠、节点溯源跳转 |

**CLI 先于 Web**：Mermaid / DOT 能在 PR、文档、终端里直接看，是验证图谱正确性的最快通道。Web 画布解决的是"好看与好用"，不是"对不对"。

---

## 6. LLM 任务规格

### 6.1 通用接口

```ts
interface LlmTask<In, Out> {
  readonly id: string;          // 'L1' .. 'L6'
  readonly enabled: boolean;    // feature flag，默认 false
  /** 输入必须有界：由调用方裁剪好，禁止传整个仓库 */
  buildPrompt(input: In): { system: string; user: string };
  /** 输出必须可校验 */
  readonly schema: z.ZodType<Out>;
  /** 必须有确定性兜底；返回 null 表示放弃该项，标记未识别 */
  fallback(input: In): Out | null;
  readonly budget: { maxInputTokens: number; timeoutMs: number; retries: number };
  /** 同输入不重复调用 */
  cacheKey(input: In): string;
}
```

三条硬约束：

1. **`fallback` 是必填字段** —— 由 TypeScript 在编译期强制。写不出 fallback 的任务，说明它本该是确定性能力，不该出现在这个接口里。
2. **`enabled` 默认 `false`** —— v1 全部关闭，先跑纯确定性基线。
3. **所有调用写入 `llm_call` 表** —— 可归因、可对比、可回放（§8.2）。

### 6.2 任务清单

| ID | 任务 | 触发条件 | 输入边界 | 输出 schema | fallback | 调用量级 |
| --- | --- | --- | --- | --- | --- | --- |
| **L1** | 契约语义对齐 | 3.4 中 ID 相等与规范化相等均不成立，但存在候选集 | 前端调用点（URL、方法、所在文件片段）+ 后端候选 API 列表（**≤5 条**） | `{ apiId, reason, confidence }` | 返回 `null` → 记入人工清单 | 按未命中调用点数 |
| **L2** | 动态路由解释 | 路由非静态声明（配置 / DB 驱动） | 注册点代码片段 + 相关配置片段 | `{ routes: [{ method, path, evidence[] }] }` | 记为「动态路由·未展开」 | 按动态注册点数（罕见） |
| **L3** | 动态 SQL 解释 | 表名来自字符串拼接或运行时变量 | SQL 构造代码片段 + 已抽出的字面表名线索 | `{ tables: string[] }` | 抽字面表名，动态部分标 `unknown` | 按动态 SQL 点数 |
| **L4** | 模块业务摘要 | S2 每模块一次 | 模块路径 + 文件名列表 + 主要符号名（**不传源码**） | `{ summary, tags[] }` | 目录名 + 主要符号名 | = 模块数 |
| **L5** | 链路业务说明 | S4 每模块一次 | **只传已确认的结构化链路事实，不传源码** | `{ markdown }` | 用事实拼模板文案 | = 模块数 |
| **L6** | 技术栈兜底判定 | manifest 缺失或信号矛盾 | 目录树摘要 + 配置文件清单 | `{ stack, version, evidence }` | 返回 `null` → 标 `unknown` 交人工 | 按异常项目数（罕见） |

> **注意 L4 / L5 的输入设计**：两者都**不传源码**。原因是业务含义往往已经浓缩在"API 路径 + 表名 + 符号名"里了（`/api/v1/orders` + `t_order` → 订单模块）。**由代码先把语义浓缩好，再让 LLM 做命名和表达**，既省 token 又比"让模型读代码猜业务"更准。这条直接决定了 LLM 的成本量级。

### 6.3 开关与归因

| 机制 | 说明 |
| --- | --- |
| 逐项开关 | `codeatlas.config.json` 中 `llm.tasks: { L1: false, L4: true, ... }` |
| 调用留痕 | 每次调用写 `llm_call`（`taskId` `cacheKey` `inputHash` `output` `model` `tokensIn` `tokensOut` `latencyMs` `ok` `fallbackUsed`） |
| 效果对比 | 报告输出「任务开关矩阵 × 召回率 / 误报率」对比表，回答"开这一个值不值" |
| 优雅降级 | 关闭任意任务后，其 fallback 路径自动生效，**图谱仍完整产出**，只是该部分质量下降 |

### 6.4 反蔓延准则

| # | 准则 | 违反信号 |
| --- | --- | --- |
| 1 | 写不出 fallback 的任务，不该交给 LLM | 接口层报错（编译期） |
| 2 | LLM 任务总数应是个位数 | 本设计 6 个；若涨到 15 个，说明把该代码做的事推给了模型 |
| 3 | LLM 永不直接写产物 | 出现 `graph.json` 中被 LLM 直接写的字段 |
| 4 | 调用次数在设计阶段可算出上限 | 算不出来 → 说明边界没画清，先回去画边界 |
| 5 | **不引入 LLM 编排框架**（LangChain / 同类 Agent 框架） | 代码里出现 `Chain` / `Agent` / `Tool` 抽象，或核心依赖表里出现编排框架 |

---

### 6.5 Provider 抽象与密钥边界

#### 为什么不接 LangChain（决策 D-12）

本项目**不引入任何 LLM 编排框架**。四条理由，第一条是决定性的：

| # | 理由 | 说明 |
| --- | --- | --- |
| 1 | **与架构主张冲突** | LangChain 的核心抽象（Chain / Agent / Tool / Memory）是为**自主编排**设计的；而 D-01 定的是「确定性流水线 + 受控智能，非自主 Agent」。引入它等于在架构里塞进一个反向主张 |
| 2 | **能力错配** | 我们需要的是 prompt 边界、输出 schema 校验、**强制 fallback**、token 预算、缓存键——这五件事它一件都不直接提供；它提供的 Memory / Retriever / multi-agent / tracing，我们全都不要 |
| 3 | **依赖漂移风险** | 其 JS 版本迭代快、破坏性变更多。放在 `@codeatlas/llm` 的核心位置，等于把架构稳定性押在一个高速漂移的库上 |
| 4 | **可观测性反噬** | 其 callback / tracing 体系默认存在上报通路。本设计要做出境审计，多一个不受控的出口是负债 |

**替代方案**：`LlmTask` 接口（§6.1）+ 自研运行器（约 150–250 行）+ **OpenAI 官方 SDK**。规模可控，且每一行都在我们能控制的位置。

> **预留位**：若后续发现内网模型网关协议过于特殊、OpenAI 兼容模式无法覆盖，备选引入 Vercel AI SDK 的 `generateObject(zodSchema)`——它比 LangChain 薄得多，形态与 `LlmTask` 几乎一一对应。v1.1 不引入。

#### Provider 抽象（必需项）

因为"内网私有化适配"是既定路线，**`LlmProvider` 接口不是可选项，是必需项**：

```ts
interface LlmProvider {
  readonly id: string;                                    // 'openai-compat' | 'internal-xxx'
  /** 单次结构化输出调用；throws 交由 LlmTask 的 fallback 兜底 */
  complete(req: {
    system: string;
    user: string;
    schema: JsonSchema;                                   // 由 zod 导出，交给模型侧约束
    maxInputTokens: number;
    timeoutMs: number;
  }): Promise<{ raw: unknown; usage: TokenUsage; model: string }>;
}
```

首个实现为 `OpenAiCompatProvider`（**走 OpenAI 兼容协议**）。这样做的好处是：**大量内网模型网关同样提供 OpenAI 兼容接口**，届时适配可能只是替换 `baseURL`，而不是新写一个 Provider。

#### 密钥边界（硬约束）

| 约束 | 内容 |
| --- | --- |
| **Key 的物理位置** | 只存在于**本机进程**——环境变量 `CODEATLAS_OPENAI_API_KEY` 优先，用户级 `~/.codeatlas/credentials.json` 兜底（**位于系统根之外**） |
| **Key 绝不进入产物** | 不写 `.codeatlas/` 下任何文件；`config.json` 只记录开关，不记录凭据 |
| **浏览器绝不持有 Key** | Web 画布只调用**本机服务**；由本地 Hono 服务持有 Key 并转发。因此 **LLM 调用只发生在 pipeline / 本地服务进程内**，不会出现在前端代码里 |
| **Key 绝不入库** | 系统根下的任何产物、文档、示例配置中都不出现真实凭据 |

```
浏览器画布 ──▶ 本机服务（Hono，持有 Key）──▶ OpenAI API
                    ▲
              CLI 进程（复用同一 Provider）
```

#### 数据出境与最小化

走云端 API 意味着**代码片段会离开本机**，因此：

1. **出境内容有界**：LLM 任务只发送**候选集相关的片段**（§6.1 `buildPrompt` 必须保证输入有界），不发送整个文件、不发送目录树、不发送仓库元信息。
2. **`audit/` 兼作出境审计**：每次调用记录 taskId、model、token 用量、输入哈希与**实际发送的 payload**，用于事后回答"到底发出去了什么"。
3. **"默认关闭"是合规姿态，不只是归因手段**：6 个 LLM 任务默认 `enabled: false`（D-06）。这意味着**默认状态下代码完全不出本机**——这是对使用方最好用的说法，也是把工具引入受限环境的默认安全起点。
4. ⚠️ **开发阶段提示**：用真实业务仓直接跑并开启 LLM，等于把真实业务代码发往境外 API。开发与验证阶段建议使用练习项目或脱敏代码。

---

## 7. 四道闸门与证据回验

### 7.1 闸门总览

**所有候选事实**（无论来自提取器还是 LLM）在写入事实源之前，必须依次通过四道闸门：

| # | 闸门 | 校验内容 | 不通过的处理 |
| --- | --- | --- | --- |
| **一** | Schema 校验 | 按 zod schema 解析 | 整体丢弃，记 `defect`，走 fallback |
| **二** | 证据回验 | 打开源文件，验证 `line` 处的 `snippet` **真实存在** | **丢弃该项**（反幻觉的核心闸门） |
| **三** | 置信度不得自升级 | LLM 产物上限 `speculative`；多来源取最低值 | 强制降级到允许值 |
| **四** | 冲突消解 | 同 `src + dst + type` 的边合并：`certain > probable > speculative`；同级取证据并集 | 保留高置信度边，低者降为备选证据 |

### 7.2 证据回验的实现

```
evidenceSnippetMatches(evidence, scopedFs) -> boolean
  1. content  = scopedFs.read(evidence.file)        // 白名单校验在内
  2. lines    = content.split('\n')
  3. window   = lines[line-1-delta .. line-1+delta+snippetLines]   // delta 默认 2
  4. return normalize(window) === normalize(evidence.snippet)
        // normalize：去首尾空白、折叠内部连续空白、统一换行符
```

**为什么这是"硬"机制**——伪造一条证据需要同时满足三个条件：

1. 文件路径在白名单内 **且** 文件真实存在
2. 行号定位到的窗口内容与声称的片段一致
3. 片段本身与源文本逐字符（归一化后）相同

三者同时为真，才可能伪造成功。门槛高到 LLM 基本不可能蒙混过关。

### 7.3 为什么闸门必须前置

图谱一旦落盘，后续所有阶段（装配、投影、渲染、增量复用）都会**无条件信任**它。事后再校验等于让污染先流到终点再回头找。**闸门必须卡在"写入事实源之前"这一刻，而不是"生成报告之后"。**

### 7.4 缺陷（Defect）分类

| 类别 | 含义 | 严重度 |
| --- | --- | --- |
| `schema-invalid` | LLM 或提取器输出不符合 schema | 低（已丢弃） |
| `evidence-mismatch` | 证据回验失败 | **高**（意味着幻觉或解析错误） |
| `out-of-scope` | `source.file` 落在白名单外 | **高**（边界守卫被突破） |
| `dangling-edge` | 边的端点节点不存在 | 中（可能是死代码或漏识别） |
| `unresolved` | 三级降级均未命中 | 中（计入覆盖率缺口） |
| `low-trust-module` | 模块缺陷率超过 50% | 高（该模块产出不可信） |

`evidence-mismatch` 与 `out-of-scope` 两类为**高严重度**，任一出现都应在报告中显著提示，不应被淹没在统计数字里。

---

## 8. 存储设计

### 8.1 双存储职责划分

> **JSON 是事实源，SQLite 是派生索引。SQLite 可以随时删掉重建，且重建结果必须与增量维护的结果一致（不变量 I6）。**

| | JSON 事实源 | SQLite 派生索引 |
| --- | --- | --- |
| 位置 | `.codeatlas/facts/` `graph/` | `.codeatlas/index.db` |
| 角色 | **唯一的真相** | 查询与推理的加速层 |
| 可 diff | ✓ 纯文本，可逐行 code review | ✗ 二进制 |
| 可版本化 | ✓ 格式上就绪（文本 + 稳定键序） | ✗ |
| 人工确认载体 | ✓（Markdown + JSON） | ✗ |
| 崩溃恢复 | 原子 rename 即可 | 可从 JSON 重建 |
| 大系统查询 | 全量加载不可行 | ✓ 索引 + 图遍历 |

**为什么不让 SQLite 当真源**：人工确认需要**可读、可 diff、可评审**的文本，二进制库做不到这一点。而 SQLite 的价值在**查询**——增量失效需要回答"哪些模块引用了这个 API"，用 SQL 现成索引是毫秒级，用 JSON 全量遍历是分钟级。

> ⚠️ **一次措辞修正（v1.1）**：此前本表写作「可入 Git ✓」，理由表述为"图谱的每一次变化都应该能进 PR 被人看到"。这个理由**隐含假设了产物被纳入版本管理**，而 v1.1 已明确：**工具不内置产物版本管理**（§1.3）。
>
> 正确的表述是："可 diff、可人工评审"是 **JSON 这种格式自带的属性**（纯文本、键序稳定、人类可读），与工具是否集成 Git 无关。至于要不要把 `.codeatlas/` 纳入某个仓库、如何管理它的历史，是**部署侧的选择**，工具既不代劳也不阻止。D-04（JSON 事实源 + SQLite 派生索引）本身不受影响。
>
> 附带推论：`.codeatlas/index.db` 作为可随时重建的派生索引，**无论部署侧是否版本化，都不应该被纳入**——若部署侧确实建立了版本管理，应忽略它。

### 8.2 SQLite 表结构

```sql
PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;

CREATE TABLE meta (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL              -- schema_version / fact_source_hash / last_full_run_at
);

CREATE TABLE system (
  id         TEXT PRIMARY KEY,
  name       TEXT NOT NULL,
  root_path  TEXT NOT NULL,
  created_at TEXT NOT NULL
);

CREATE TABLE project (
  id            TEXT PRIMARY KEY,
  system_id     TEXT NOT NULL REFERENCES system(id) ON DELETE CASCADE,
  name          TEXT NOT NULL,
  rel_path      TEXT NOT NULL,
  stack         TEXT,
  framework     TEXT,
  framework_ver TEXT,
  template_id   TEXT,
  detection     TEXT,              -- certain | probable | unknown
  profile_json  TEXT,
  detected_at   TEXT NOT NULL
);

CREATE TABLE module (
  id           TEXT PRIMARY KEY,
  project_id   TEXT NOT NULL REFERENCES project(id) ON DELETE CASCADE,
  name         TEXT NOT NULL,
  rel_path     TEXT NOT NULL,
  kind         TEXT,
  summary      TEXT,
  tags_json    TEXT,
  status       TEXT NOT NULL DEFAULT 'pending',   -- pending|confirmed|corrected|rejected
  fingerprint  TEXT NOT NULL,                     -- 见 §9.3
  defect_count INTEGER NOT NULL DEFAULT 0,
  updated_at   TEXT NOT NULL
);
CREATE INDEX idx_module_project ON module(project_id);
CREATE INDEX idx_module_status  ON module(status);

CREATE TABLE file (
  id             TEXT PRIMARY KEY,   -- 相对系统根路径
  module_id      TEXT REFERENCES module(id) ON DELETE SET NULL,
  rel_path       TEXT NOT NULL,
  sha256         TEXT NOT NULL,
  size           INTEGER NOT NULL,
  mtime          TEXT,
  parser_version TEXT NOT NULL,      -- 缓存键组成部分
  indexed_at     TEXT NOT NULL
);
CREATE UNIQUE INDEX idx_file_path ON file(rel_path);
CREATE INDEX idx_file_sha         ON file(sha256);

CREATE TABLE api (
  id             TEXT PRIMARY KEY,   -- api:<project>:<METHOD>:<normalizedPath>
  project_id     TEXT NOT NULL REFERENCES project(id) ON DELETE CASCADE,
  method         TEXT NOT NULL,
  path_raw       TEXT NOT NULL,
  path_norm      TEXT NOT NULL,
  handler_symbol TEXT,
  module_id      TEXT REFERENCES module(id) ON DELETE SET NULL,
  file_id        TEXT REFERENCES file(id) ON DELETE SET NULL,
  line           INTEGER NOT NULL,
  evidence_json  TEXT NOT NULL,
  extractor_id   TEXT NOT NULL,
  confidence     TEXT NOT NULL
);
CREATE INDEX idx_api_path ON api(path_norm, method);

CREATE TABLE data_access (
  id            TEXT PRIMARY KEY,
  project_id    TEXT NOT NULL REFERENCES project(id) ON DELETE CASCADE,
  module_id     TEXT REFERENCES module(id) ON DELETE SET NULL,
  table_name    TEXT,
  kind          TEXT NOT NULL,       -- read|write|ddl|unknown
  orm           TEXT,
  sql_hash      TEXT,
  file_id       TEXT REFERENCES file(id) ON DELETE SET NULL,
  line          INTEGER NOT NULL,
  evidence_json TEXT NOT NULL,
  extractor_id  TEXT NOT NULL,
  confidence    TEXT NOT NULL
);
CREATE INDEX idx_da_table  ON data_access(table_name);
CREATE INDEX idx_da_module ON data_access(module_id);

CREATE TABLE component (
  id            TEXT PRIMARY KEY,
  project_id    TEXT NOT NULL REFERENCES project(id) ON DELETE CASCADE,
  module_id     TEXT REFERENCES module(id) ON DELETE SET NULL,
  name          TEXT NOT NULL,
  route_path    TEXT,
  file_id       TEXT REFERENCES file(id) ON DELETE SET NULL,
  line          INTEGER,
  evidence_json TEXT,
  confidence    TEXT NOT NULL
);

CREATE TABLE link (
  id            TEXT PRIMARY KEY,
  module_id     TEXT REFERENCES module(id) ON DELETE CASCADE,
  src_node_id   TEXT NOT NULL,
  dst_node_id   TEXT NOT NULL,
  edge_type     TEXT NOT NULL,
  confidence    TEXT NOT NULL,
  produced_by   TEXT NOT NULL,       -- extractorId 或 llmTaskId ← 归因字段
  evidence_json TEXT NOT NULL,
  based_on_json TEXT,                -- 依赖契约面哈希，增量用
  confirmed     INTEGER NOT NULL DEFAULT 0,
  created_at    TEXT NOT NULL
);
CREATE INDEX idx_link_src ON link(src_node_id);
CREATE INDEX idx_link_dst ON link(dst_node_id);

CREATE TABLE node (
  id          TEXT PRIMARY KEY,
  system_id   TEXT NOT NULL REFERENCES system(id) ON DELETE CASCADE,
  node_type   TEXT NOT NULL,
  name        TEXT NOT NULL,
  attrs_json  TEXT,
  source_file TEXT NOT NULL,
  source_line INTEGER
);

CREATE TABLE edge (
  id            TEXT PRIMARY KEY,
  system_id     TEXT NOT NULL REFERENCES system(id) ON DELETE CASCADE,
  dim_id        TEXT NOT NULL,
  src_id        TEXT NOT NULL,
  dst_id        TEXT NOT NULL,
  edge_type     TEXT NOT NULL,
  confidence    TEXT NOT NULL,
  produced_by   TEXT NOT NULL,
  evidence_json TEXT NOT NULL,
  dangling      INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_edge_src ON edge(src_id);
CREATE INDEX idx_edge_dst ON edge(dst_id);
CREATE INDEX idx_edge_dim ON edge(dim_id);

CREATE TABLE snapshot (
  id            TEXT PRIMARY KEY,
  system_id     TEXT NOT NULL REFERENCES system(id) ON DELETE CASCADE,
  baseline_id   TEXT NOT NULL,        -- 多仓基线 ID：内容寻址，全局唯一（见 §9.1）
  graph_version TEXT NOT NULL,
  graph_hash    TEXT NOT NULL,
  stats_json    TEXT,
  created_at    TEXT NOT NULL
);
CREATE UNIQUE INDEX idx_snapshot_baseline ON snapshot(baseline_id);
CREATE INDEX idx_snapshot_system ON snapshot(system_id, created_at);

-- 基线逐仓明细：一个 baseline 对应 N 行（系统根下的每个项目一行）
CREATE TABLE baseline_project (
  baseline_id   TEXT NOT NULL REFERENCES snapshot(baseline_id) ON DELETE CASCADE,
  project_id    TEXT NOT NULL,        -- 逻辑名，由 rel_path 派生（改目录名 = 结构变更）
  rel_path      TEXT NOT NULL,        -- ★ 相对【系统根】，非子仓根（不变量 I9）
  vcs           TEXT NOT NULL,        -- git | none
  commit_sha    TEXT,                 -- vcs = none 时为 NULL
  branch        TEXT,
  dirty         INTEGER NOT NULL DEFAULT 0,
  tree_hash     TEXT,                 -- vcs = none 时的内容哈希锚点（精度降级）
  precision     TEXT NOT NULL,        -- exact | degraded
  PRIMARY KEY (baseline_id, project_id)
);
CREATE INDEX idx_baseline_project_path ON baseline_project(rel_path);

CREATE TABLE changeset (
  id                    TEXT PRIMARY KEY,
  system_id             TEXT NOT NULL REFERENCES system(id) ON DELETE CASCADE,
  from_baseline_id      TEXT NOT NULL,
  to_baseline_id        TEXT NOT NULL,
  changed_files_json    TEXT NOT NULL,   -- 路径基准为系统根（I9）
  affected_modules_json TEXT NOT NULL,
  rerun_set_json        TEXT NOT NULL,
  escalated             INTEGER NOT NULL DEFAULT 0,   -- 是否升级为全量
  escalate_reason       TEXT,
  created_at            TEXT NOT NULL
);

CREATE TABLE llm_call (
  id            TEXT PRIMARY KEY,
  task_id       TEXT NOT NULL,       -- L1..L6
  cache_key     TEXT NOT NULL,
  input_hash    TEXT NOT NULL,
  output_json   TEXT,
  model         TEXT,
  tokens_in     INTEGER,
  tokens_out    INTEGER,
  latency_ms    INTEGER,
  ok            INTEGER NOT NULL,
  fallback_used INTEGER NOT NULL DEFAULT 0,
  created_at    TEXT NOT NULL
);
CREATE UNIQUE INDEX idx_llm_cache ON llm_call(cache_key);

CREATE TABLE defect (
  id          TEXT PRIMARY KEY,
  module_id   TEXT REFERENCES module(id) ON DELETE CASCADE,
  kind        TEXT NOT NULL,
  severity    TEXT NOT NULL,
  detail_json TEXT,
  resolved    INTEGER NOT NULL DEFAULT 0,
  created_at  TEXT NOT NULL
);
CREATE INDEX idx_defect_kind ON defect(kind, severity);

CREATE TABLE audit (
  id      TEXT PRIMARY KEY,
  ts      TEXT NOT NULL,
  actor   TEXT NOT NULL,
  action  TEXT NOT NULL,
  path    TEXT,
  allowed INTEGER NOT NULL,
  reason  TEXT
);
```

### 8.3 一致性保障

| 机制 | 说明 |
| --- | --- |
| 重建命令 | `codeatlas index rebuild` 从 `facts/` 完全重建 `index.db` |
| 一致性测试 | 重建后的 DB 与增量维护的 DB，在所有业务查询上结果必须一致（**I6 的可测试化**） |
| 原子写 | 所有 JSON 写入走 `tmp + rename`（§11.2） |
| 索引版本 | `meta.schema_version` 不匹配时自动触发重建 |

### 8.4 产物目录布局

```
<system-root>/
└── .codeatlas/
    ├── system.json              # 系统声明（唯一人工维护的配置，见下方 rootDir 说明）
    ├── config.json              # 运行配置（LLM 开关等；不含任何密钥）
    ├── facts/                   # ---- JSON 事实源：文本、可 diff、可评审 ----
    │   ├── whitelist.json
    │   ├── tech-profile.json
    │   ├── module-index.json
    │   ├── link/<moduleId>.json
    │   └── confirm/<moduleId>.md        # 供 S4 人工确认
    ├── graph/
    │   ├── graph.json
    │   └── dimensions/d1..d6.json
    ├── snapshots/<baselineId>.json        # 多仓基线快照（非单个 commitSha）
    ├── changesets/<fromBaselineId>-<toBaselineId>.json
    ├── audit/YYYY-MM-DD.jsonl
    └── index.db                 # ---- SQLite 派生索引：可删重建 ----
```

**三条与产物位置相关的约定**：

1. **`snapshots/` 以 `baselineId` 命名，不以 `commitSha` 命名。** 系统根下可能有多个独立仓，不存在"系统的单一提交号"。`baselineId` 由其 `versionBaseline.projects[]` 全体内容寻址派生（见 §9.1）。
2. **密钥不落 `.codeatlas/`。** `.codeatlas/` 下的内容按本设计属于"可评审的文本产物"，而 API Key 属于凭据。凭据只走**进程环境变量**（`CODEATLAS_OPENAI_API_KEY`），或用户级目录 `~/.codeatlas/credentials.json`——**位于系统根之外**，不会随产物被复制、打包或评审。
3. **`.codeatlas/system.json` 中的 `rootDir` 不再存放绝对路径。** 系统根可由 `.codeatlas/` 所在位置反推（`..`），存放绝对路径既冗余，又会让该文件**换机器即失效**、无法随代码分发。`rootDir` 降级为可选的覆盖项，仅在"产物目录与系统根不同级"的非常规布局下使用。

---

## 9. 增量失效算法

### 9.1 变更集推导

**前提**：被分析系统的代码以**完整 Git 仓**形态提供（含 `.git` 提交历史）。但系统根下可能有**多个互相独立的仓**（外加可能没有版本管理的项目），因此推导必须**逐仓进行后合并**——不能用一条 `git diff` 覆盖整个系统。

```
0. 采集目标基线：对系统根下每个项目判定 VCS 根（「最近 VCS 根」原则，§2.4）
   → Baseline { projects: [ {projectId, relPath, vcs, commit, dirty, treeHash} ] }
   → baselineId = H(projects 的规范化序列)        // 内容寻址，与 §8.4 的 snapshots 命名对应

1. 逐仓推导变更（所有路径归一化为【相对系统根】，不变量 I9）：
   ∀ p ∈ Baseline.projects:
     vcs = git  → git -C <p.relPath> diff --name-status --find-renames
                    <prev.commit>..<p.commit>
                  → 变更条目路径前缀拼接 p.relPath
     vcs = none → 目录树哈希比对（逐文件 sha256 差异，全量扫描）
                  → 标记 precision = degraded
                  → 且【无法识别重命名】：R 只能退化为 D + A
   → 合并各仓结果得 D（变更文件集，含 A/M/D/R）

2. ∀ d ∈ D：查 file 表 → 所属 module m → A₀ = ∪ { m }

3. 结构敏感检测（命中任一 → 置全局失效信号，升级全量）：
   - 依赖清单：package.json / pom.xml / build.gradle / requirements.txt / go.mod
   - 工作区与构建配置：bunfig.toml / tsconfig*.json / vite.config.* / settings.gradle
   - 项目新增或删除（系统根下顶层目录数量变化）
   - 【多仓新增】子项目路径变更 / 子仓增删 / 某项目 vcs 类型变化（git ↔ none）
   - 【多仓新增】任一项目由 clean 变为 dirty（版本锚点不再能描述真实代码状态）
   - .codeatlas/system.json 自身变更
   - 提取器版本或技术栈模板版本升级
```

**三个由多仓形态引入的注意点**：

1. **不存在"系统的单一提交号"**。因此 §8.4 的 `snapshots/` 以 `baselineId` 命名，而不是 `commitSha`；`changeset` 记录 `fromBaselineId → toBaselineId`。`baselineId` 由全部项目的版本锚点序列内容寻址派生——任何**一个**子仓前进一个提交，`baselineId` 就变化一次，语义上正确。
2. **无版本管理的项目是精度短板**。它无法识别重命名、只能全量扫描目录树，且 `precision = degraded` 必须**在图谱与报告的显式位置暴露**（沿用原 R8 的处置方式）。若系统根下这类项目占比高，增量收益会显著下降——这属于**上报给使用者的结论**，而不是工具内部悄悄降级。
3. **首次运行没有 `prev` 基线**，等价于全量。`--dry-run` 在无基线时可提示"将执行全量"。

> **dirty 的处理策略尚未定稿**（原 Q14）。倾向：默认**拒绝运行**并要求先提交或 stash，需显式 `--allow-dirty` 才放行——理由与 I8 同源，宁可慢一次，不要错一版。此项随 M3 一并确定。

### 9.2 影响扩散（必须跨项目）

```
A ← A₀ ;  队列 ← A₀
while 队列非空:
    m ← 队列.pop()
    // ① 契约扩散：m 暴露的 API 的所有消费者
    for api ∈ apis(m):
        for consumer ∈ reverseIndex[api.id]:
            if consumer ∉ A: A.add(consumer); 队列.push(consumer)
    // ② 数据耦合扩散：m 访问的表的所有其他访问者
    for t ∈ tables(m):
        for other ∈ reverseIndex[t]:
            if other ∉ A: A.add(other); 队列.push(other)
    // ③ 共享库扩散
    for ref ∈ reverseIndex[module:m]:
        if ref ∉ A: A.add(ref); 队列.push(ref)
```

**① 是跨项目的**：后端改了一个 API，前端消费它的模块必须一起重校验。这是"图上的影响扩散"区别于"文件哈希比对"的核心所在。

### 9.3 模块指纹（缓存键）

```
fingerprint(module) = H(
      ⊕ H(该模块下所有文件的 sha256)
    + H(提取器版本 extractorVersion)
    + H(模板版本 templateVersion)
    + H(该项目 tech-profile 的哈希)
    + H(依赖契约面 dependentContractSurface)     ← 最容易漏的一条
)
```

`dependentContractSurface` 的定义：

| 模块角色 | 契约面内容 |
| --- | --- |
| 前端模块 | 它调用的所有后端 API 的**规范化签名集合**（method + normalizedPath + 参数结构摘要） |
| 后端模块 | 它访问的所有表的 **DDL 摘要**（列名 + 类型） |
| 被依赖模块 | 它对外暴露的符号签名摘要 |

> **为什么必须包含这一项**（不变量 I7）：假设后端把 `POST /orders` 的请求体从 `{ sku, qty }` 改成 `{ sku, quantity }`，前端代码一行未动。如果前端模块的指纹只看自己的源码哈希，它会**命中缓存直接跳过**，跨项目契约永远得不到重校验。这是最隐蔽的一类漏报——图看着是完整的，但内容已经过期。

### 9.4 保守升级规则

| 情况 | 判定 |
| --- | --- |
| 结构敏感文件变更 | **升级全量** |
| 无法定位变更文件所属模块 | **升级全量** |
| 非 Git 项目（哈希降级路径） | **升级全量**，并在报告标注"精度下降" |
| `--dry-run` 得到的重跑集 > 全量模块数的 60% | **升级全量** |
| 扩散链条深度 > 8 层 | **升级全量**（疑似误连边导致爆炸） |

一句话准则：**宁可慢一次，不要错一版。** 增量漏报的代价远高于多跑一次全量。

### 9.5 重跑集与等价性

| 阶段 | 增量时的处理 |
| --- | --- |
| S0 | 仅当 `system.json` 变更时重跑，否则复用 |
| S1 | 仅重跑变更文件所属项目 |
| S2 | 仅重跑受影响模块所属项目 |
| S3 | **仅重跑 A 中的模块**（核心收益点） |
| S4 | 状态保留；**被重跑过的模块状态回退为 `pending`** |
| S5 S6 S7 | **恒全量**（纯投影，便宜且必须一致） |

> S4 这条容易漏：模块代码变了，人确认过的结论就过期了。**变更必须使确认失效**，否则图谱会带着"已被确认"的假象继续传播错误。

**等价性验证（G2）**：`codeatlas verify --incremental-vs-full` —— 对同一目标基线分别用增量和全量产出 `graph.json`，节点集与边集必须完全一致，否则判为缺陷。

---

## 10. 范围守卫设计

### 10.1 单点收口

所有文件访问经 `ScopedFs`，它是 scope 包唯一导出的 IO 入口。

```ts
class ScopedFs {
  constructor(roots: ResolvedRoot[], auditor: Auditor) {}

  async read(rel: string): Promise<string>;      // 越界 → throw ScopeViolation
  async glob(patterns: string[]): Promise<string[]>;
  async stat(rel: string): Promise<Stat>;
  async realpath(rel: string): Promise<string>;
}
```

- 所有路径先 `resolve` 再 `realpath`，然后做 `startsWith(root)` 校验
- 越界**不静默**：写审计 + 抛 `ScopeViolation`
- 阶段代码**物理上拿不到** `fs`（由 §3.3 的架构测试强制）

### 10.2 四层防御

| 层 | 机制 | 拦什么 | 代码落点 |
| --- | --- | --- | --- |
| **L1** 路径层 | 白名单外读取直接拒绝 + 审计 | 提取器"顺手"读隔壁目录 | `ScopedFs` |
| **L2** 依赖层 | 系统外引用建 `external` 节点，不展开内部 | 顺着 import / API 调用爬出去 | S3.5 依赖解析器 |
| **L3** 产物层 | 装配时校验所有 `source.file` 在白名单内，越界即剔除 | 前两层漏网的 | S5 装配 |
| **L4** 链接层 | `realpath` 后再校验，防软链逃逸 | 用软链绕过路径检查 | `ScopedFs.realpath` |

**L3 是最后一道闸**——它保证图谱里每个节点都能自证"我属于这个系统"，这是防污染的根本保障，而不是靠流程自觉。

### 10.3 边界节点策略

| 类别 | 是否建节点 | 理由 |
| --- | --- | --- |
| 第三方公共包（npm / Maven / pip） | ❌ 不建 | 平台能力，全建进来图谱会被噪声淹没 |
| 外部系统（他方服务） | ✓ `external:service:*` | 需要知道系统依赖了谁 |
| 共享库（自研跨系统包） | ✓ `external:lib:*` | 其变更会同时影响多个系统 |
| **共享数据库表** | ✓ 标 `shared: true` | **最隐蔽的跨系统耦合**：不体现为任何 API 调用 |
| 本系统内的其他项目 | ✓ 正常节点 | 属于边界内 |

---

## 11. 错误处理与断点续跑

### 11.1 Result 而非抛异常

```ts
type Result<T> =
  | { ok: true;  value: T }
  | { ok: false; defect: Defect };
```

一个万文件级别的长跑，单个文件解析失败不该中断整轮。每个文件级操作返回 `Result`，失败收集进 `defect` 表，轮末汇总成报告。

**例外**：`ScopeViolation` 与 `SchemaInvalid`（系统级配置错误）**应当抛出并中止**——因为它们意味着前提假设已经被打破，继续跑只会产出垃圾。

### 11.2 原子写

```
写 facts/link/<moduleId>.json:
  1. write   → facts/.tmp/<uuid>.json
  2. fsync
  3. rename  → facts/link/<moduleId>.json      // 同文件系统内 rename 是原子的
```

保证任意时刻 kill 进程都不会留下半截 JSON。

### 11.3 断点续跑

| 机制 | 说明 |
| --- | --- |
| 检查点粒度 | **每个模块**（S3 完成后立即落盘 + 更新 `module.fingerprint`） |
| 恢复 | `codeatlas run --resume`，`fingerprint` 未变的模块直接跳过 |
| 进度可见 | `codeatlas status` 输出模块完成率、缺陷率、待确认数 |
| 幂等 | S0–S4 均幂等，重复执行不产生重复事实 |

---

## 12. 技术选型

### 12.1 选型总表

| 关注点 | 选型 | 理由 |
| --- | --- | --- |
| **运行时** | **Bun** | TS 直跑（调试 S3 提取链时无 tsx/ts-node 中间层）；启动快（CLI 是高频工具） |
| **工具链** | **Bun 内置**（`bun install` / `bun test` / `bun build`） | 一套工具链，零原生编译；取舍与回退路径见 §3.1 |
| **语言** | **TypeScript**（`strict`，project references） | — |
| **Schema / 校验** | **zod** → 导出 JSON Schema | 单一真源，消灭类型漂移（D-05） |
| **多语言 AST** | **`web-tree-sitter` 0.27** + **各语言官方语法包**（`tree-sitter-<lang>` 的 WASM） | 纯 TS 无原生编译；一套接口覆盖 Java / Python / TS。✅ **已在 Bun 上验证通过**；⚠️ **禁用 `tree-sitter-wasms` 聚合包**（段格式过旧，非 Bun 问题），见 §12.2 |
| Vue / JSX 解析 | `@vue/compiler-sfc` + `@babel/parser` | SFC 与 JSX 专用解析器比通用 AST 更准（Vue 不走 tree-sitter） |
| SQL 解析 | `node-sql-parser`（**v5 用 `astify()`**，已非 `ast()`） | 抽取裸 SQL 的表名与操作类型 |
| 图数据结构 | **graphology** | TS 类型友好，布局算法生态全 |
| 布局算法 | **elkjs** / dagre | 分层布局最适合调用链。⚠️ **必须显式传 `workerFactory`**：Bun 用内置 `Worker`，Node 用 elkjs 自带进程内 worker，不能依赖自动探测（见 §12.2） |
| **持久化索引** | **`bun:sqlite`** | 内置，**免原生编译**（`better-sqlite3` 换 Node 大版本需 rebuild）；经 `IndexStore` 接口暴露，业务代码不直接 import |
| **VCS 适配** | **`VcsAdapter` 接口**，首个实现调 `git` 二进制 | 不写死 Git（"对方使用的版本管理工具"可能不止 Git）；无 VCS 项目走目录树哈希降级 |
| CLI | **clipanion** | 类型安全的命令与参数定义；与 CLI 进程形态无关，**不需要任何 HTTP 框架** |
| **Web 服务** | **Hono** | TS 类型推导优于 Express；多运行时；本地 API 服务仅数个端点 |
| **前端** | **Vue 3 + Vite** | 团队既有栈 |
| **Web 画布** | **AntV G6** | 自定义节点、层级折叠、大图性能（D-10） |
| **LLM 编排** | **自研 `LlmTask` 运行器 + OpenAI 官方 SDK** | **不接 LangChain**（D-12，理由见 §6.5） |
| **LLM Provider** | `LlmProvider` 接口，首个实现 `OpenAiCompatProvider` | 内网适配是既定路线，接口为必需项；兼容协议让内网适配可能只需换 `baseURL` |
| 测试 | **`bun test`** + 快照测试（提取器） | 提取器是纯函数，天然适合快照 |
| 架构约束 | **dependency-cruiser** | 把 §3.2 的依赖矩阵变成 CI 规则 |
| 代码规范 | ESLint + Prettier + `tsc --strict` | — |

### 12.2 运行时选型与前置验证结论

选 Bun 的收益清晰（TS 直跑、内置 SQLite / 测试 / 打包、启动快），代价是它需要排除一个 **go/no-go 级的兼容性风险**：

> `web-tree-sitter`（WASM）能否在 Bun 上正常加载并解析？
> 它是整个提取层的根基——§12.1 的多语言 AST 全靠它，Java / Python / TS 三条链路的解析都建立在它之上。

**✅ 验证结论（2026-09-23，Bun 1.3.14 / Darwin arm64）：GO —— 全押 Bun。**

同一份探针在 Bun 与 Node 22 上分别执行，**7/7 项逐项一致**：WASM 运行时初始化、三语言语法包加载、解析与遍历（**0 个 ERROR/MISSING 节点**）、tree-sitter Query 提取、字节流加载、300 次重复解析稳定性（**5684 次/秒，节点数零漂移**）、并发加载。

| 结果分支 | 实际结论 |
| --- | --- |
| 加载与解析正常 → 全押 Bun，**不再编写任何运行时兼容层** | ✅ **命中此项** |
| 加载失败 → 退回 Node.js 22 LTS（切换成本 ≈ 0） | ❌ 不触发 |

> **不新增兼容层**是这次验证的直接产出：风险既然是前置排除的，通过之后就没有理由再写防御性代码。完整数据见 [`docs/03-开发/M0-前置验证报告-tree-sitter-WASM兼容性.md`](../03-开发/M0-前置验证报告-tree-sitter-WASM兼容性.md)。

#### 12.2.1 ⚠️ 强制约束：语法包来源

验证过程中命中的一个**与运行时无关的真实陷阱**，必须写入依赖规范：

`tree-sitter-wasms`（聚合包，看似覆盖 36 种语言）**已停更**，其 `.wasm` 携带旧版 `dylink` 段名，而 `web-tree-sitter@0.27` 要求 `dylink.0`，会导致 `Language.load()` 失败 —— 且**抛出的异常 message 为空**，极易被误判为 Bun 故障。

| 来源 | 判定 |
| --- | --- |
| `tree-sitter-<lang>`（各语言官方包） | ✅ **唯一采用**，随上游同步更新 |
| `@vscode/tree-sitter-wasm` | ✅ 备选（VS Code 维护） |
| `tree-sitter-wasms` | ❌ **禁用** |

> 该失败在 Node 上**同样发生**。这正是验证时必须设置 Node 对照组的价值 —— 否则会把一个依赖陈旧问题误记为运行时缺陷，进而错误地放弃 Bun。

#### 12.2.2 SQLite 驱动的隔离（澄清）

即便全押 Bun，`pipeline` 也不应直接 `import "bun:sqlite"`——因为 `store` 包本来就导出 `IndexStore` 接口（§3.1），用什么驱动是它内部实现。这是**已定的分层要求，不是为兼容新增的成本**。

---

## 13. 落地路线

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| **M0 地基** | ① ~~tree-sitter WASM on Bun 兼容性验证（go/no-go）~~ ✅ **已完成（2026-09-23，GO）**；② monorepo 骨架、`core` 领域模型 + zod schema、`ScopedFs`、`Store`（JSON + SQLite schema）、`StageRunner`、dependency-cruiser 规则 | ~~tree-sitter 在 Bun 上跑通~~ ✅ **已达成**（7/7，与 Node 逐项一致）；② 架构测试全绿；`core` 零 IO 依赖。**运行时定标 Bun，不写兼容层**（见 §12.2） |
| **M1 竖切** | 单一栈（Vue3 + Spring Boot）打通 S0→S1→S2→S3(静态部分)→S5→S6→S7(CLI/Mermaid) | **一条完整链路可溯源到行号**；量化确定性基线召回率 |
| **M2 铺栈** | 技术栈模板扩展到 6–8 个常用栈，提取器补全 | 每个新增栈只增加模板数据，核心零改动 |
| **M3 增量** | `VcsAdapter`（首个实现为 Git）、**多仓基线采集**、逐仓 changeset 合并、fingerprint、影响扩散、等价性验证、`dirty` 策略定稿 | `--incremental-vs-full` 一致率 100%；无 VCS 项目占比与精度降级在上报中显式可见 |
| **M4 智能** | 6 个 LLM 任务（默认关闭）+ `LlmProvider`（`OpenAiCompatProvider`）+ 开关矩阵 + 效果对比报告 + 出境审计 | 每个任务可独立开关且能给出召回率增量；`audit/` 可还原每次出境的 payload |
| **M5 可视化** | Web 画布（AntV G6）、维度切换、层级折叠、节点溯源跳转 | 六维度可切换，点击节点跳回源码 |
| **M6 闸门打磨** | confirm 文档体验、批量确认、修正回写 | 单模块确认耗时 < 3 分钟 |

> **M1 是整个项目最关键的一步**。它验证的不是"能不能跑通"，而是"**提取精度到底够不够用**"。如果竖切跑完发现一条链路的召回率只有 50%，那说明模板设计或提取策略需要返工——这个结论越早知道越好，横向铺开 8 个技术栈之后才发现就晚了。

---

## 14. 风险与决策记录

### 14.1 风险清单

| # | 风险 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| R1 | 提取器覆盖不足，召回率不达标 | 图谱不完整，价值受损 | M1 竖切先量化基线；模板驱动快速补栈 |
| R2 | 模块划分粒度过粗或过细 | 增量效率低 / 确认负担重 | 默认业务域级；粒度可配置；报告暴露模块数 |
| R3 | 影响扩散爆炸（误连边导致） | 增量退化为全量 | 深度阈值 8 层；超限即升级全量 |
| R4 | LLM 幻觉绕过闸门 | 图谱被污染 | 证据回验三重条件 + 置信度上限 + 全程留痕 |
| R5 | 大系统下索引重建慢 | 首次运行或修复耗时 | 增量维护索引；重建进度可见 |
| R6 | 前端 URL 高度动态（运行时拼接） | 契约匹配失败率高 | 路径规范化 + L1 候选集裁决 + 未识别清单 |
| R7 | 人工确认成为流程瓶颈 | 工具推不动 | 按模块分批确认；优先确认高风险/高价值模块 |
| R8 | 部分项目**无版本管理**，精度下降 | 增量收益打折；无法识别重命名 | 目录树哈希降级路径 + `precision: degraded` 在上报中显式暴露；无 VCS 占比过高时主动提示使用者（形态前提见 D-15，此项概率已降低） |
| ~~R9~~ | ~~`web-tree-sitter`（WASM）在 Bun 上不可用~~ | — | ✅ **已排除（2026-09-23）**：Bun 1.3.14 上 7/7 通过，与 Node 22 逐项一致。GO（§12.2 / [验证报告](../03-开发/M0-前置验证报告-tree-sitter-WASM兼容性.md)） |
| **R10** | Bun 生态边缘兼容问题（部分依赖行为差异） | 局部功能异常、排查成本 | M0 已对 M0–M3 选型做整体 smoke（**9/9 通过**）。已登记两处实测差异，均为**用法差异而非阻断**：① `node-sql-parser` v5 改用 `astify()`；② `elkjs` 必须显式传 `workerFactory`（Bun/Node 的可用方式相反）。核心包不直接依赖 Bun 专有 API（SQLite 经 `IndexStore` 接口，工具链可退回 pnpm） |
| **R11** | LLM 出境带来数据合规风险 | 工具无法在受限环境落地 | **默认关闭**（不启用 LLM 时零出境）+ 输入有界（只发候选片段）+ `audit/` 出境留痕 + Provider 可替换 |

### 14.2 决策记录（ADR）

| # | 决策 | 结论 | 理由 | 日期 |
| --- | --- | --- | --- | --- |
| D-01 | 架构形态 | 确定性流水线 + 受控智能，**非自主 Agent** | 可复现、可增量、可归因、成本可控 | 2026-09-13 |
| D-02 | 交付形态 | CLI + Web 双端 | 用户确认 | 2026-09-11 |
| D-03 | 工程结构 | **monorepo**（**Bun workspaces** + TS project references） | 用户决定；避免后期迁移成本高于早期搭建成本 | 2026-09-18<br>（v1.1 修订） |
| D-04 | 持久化 | **JSON 事实源 + SQLite 派生索引** | 兼顾可 diff/可评审与查询性能 | 2026-09-18 |
| D-05 | Schema 真源 | **zod → 导出 JSON Schema** | 消灭类型漂移 | 2026-09-18 |
| D-06 | LLM 启用策略 | 6 个任务全部实现，**默认关闭** | 先拿确定性基线，效果可归因；同时构成"零出境"的默认安全姿态 | 2026-09-18 |
| D-07 | 节点 ID | **内容寻址**，禁行号 / 自增 / UUID / 绝对路径 | 增量可对齐 | 2026-09-18 |
| D-08 | 阶段重跑粒度 | S1/S2/S3 增量，**S5/S6/S7 恒全量** | 投影便宜且必须一致，增量化得不偿失 | 2026-09-18 |
| **D-09** | **系统根定义** | 系统根 = **容器文件夹**（内部各项目可各自独立 VCS 仓）；`.codeatlas/` 为其下的**旁挂产物目录**；工具仓与系统根**解耦** | 使用者明确"只能保证系统所有项目在同一个文件夹内，不保证同一个仓库" | 2026-09-18 |
| **D-10** | **Web 画布** | **AntV G6** | 自定义节点、层级折叠、大图性能均满足六维度展示需求 | 2026-09-18 |
| **D-11** | **运行时与工具链** | **Bun**（`bun install` / `bun test` / `bun build`） | TS 直跑无中间层、内置 SQLite / 测试 / 打包、启动快。**前置条件已验证通过**（2026-09-23：tree-sitter WASM 7/7，与 Node 逐项一致）→ 定标 Bun，不再编写运行时兼容层 | 2026-09-18<br>**09-23 验证** |
| **D-12** | **LLM 编排** | **不接入 LangChain** 或同类编排框架；自研 `LlmTask` 运行器 + OpenAI 官方 SDK | ① 与"确定性流水线 + 受控智能"主张冲突；② 能力错配；③ 依赖漂移风险；④ tracing 默认有上报通路，与出境审计冲突。详见 §6.5 | 2026-09-18 |
| **D-13** | **LLM 接入与密钥边界** | 本地**直连 API**；首个 Provider 为 `OpenAiCompatProvider`；**Key 只存在于本机进程**，绝不下发浏览器；内网适配后续再议 | 内网私有化是既定方向，故 `LlmProvider` 抽象为必需项；采用 OpenAI 兼容协议可使内网适配可能仅需替换 `baseURL` | 2026-09-18 |
| **D-14** | **产物版本管理** | **不内置**（原 Q13 撤回） | VCS 在本设计中仅作为**输入侧的变更传感器**；对 `.codeatlas/` 自身做版本化会引入仓库管理职责，并使产物目录定位含混。详见 §1.3 | 2026-09-18 |
| **D-15** | **代码交付形态** | 被分析系统以**完整 Git 仓**（含 `.git` 提交历史）形态提供 | 增量可走真正的 commit diff，支持重命名识别；无版本管理的项目保留哈希降级路径兜底（§9.1） | 2026-09-18 |
| **D-16** | **WASM 语法包来源** | 采用**各语言官方单语言包** `tree-sitter-<lang>`（或 `@vscode/tree-sitter-wasm`）；**禁用聚合包 `tree-sitter-wasms`** | 该聚合包已停更，其 wasm 使用旧版 `dylink` 段名，与 `web-tree-sitter@0.27` 要求的 `dylink.0` 不匹配 → `Language.load()` 失败且**异常消息为空**，极易误判为运行时故障。**此坑与运行时无关，Node 上同样触发**（§12.2.1） | 2026-09-23 |

### 14.3 待确认事项

| # | 状态 | 事项 | 结论 / 影响范围 |
| --- | --- | --- | --- |
| Q9 | ✅ 已关闭<br>2026-09-18 | 系统根与 `.codeatlas/` 的物理关系 | **系统根 = 容器文件夹**，内部各项目可各自独立 VCS；`.codeatlas/` 旁挂于系统根下。详见 §2.4 |
| Q10 | ✅ 已关闭<br>2026-09-18 | Web 画布选型 | **AntV G6**（D-10） |
| Q11 | ⏳ 开放<br>（M2 前需定） | 首期技术栈优先级清单（按团队实际在用栈排序） | M2 铺栈顺序；此处应由使用者给出，而非设计者猜测 |
| Q12 | ✅ 已关闭<br>2026-09-18 | LLM Provider 与数据出境 | 本地**直连 API**（开发阶段 OpenAI + API Key）；内网适配后续再议。密钥边界见 §6.5、决策见 D-13 |
| Q13 | ❌ 已撤回<br>2026-09-18 | 对 `.codeatlas/` 产物做版本管理 | **不内置**，后续再议（D-14）。**注意：这与"用 VCS 做增量"是两回事**，后者保留 |
| Q14 | ⏳ 开放<br>（M3 前需定） | 子仓存在未提交改动（`dirty`）时的行为 | 拒绝运行（需 `--allow-dirty`）vs 降级记录。倾向前者，理由与 I8 同源。见 §9.1 末注 |
| Q15 | ✅ 已关闭<br>2026-09-18 | 被分析系统的代码交付形态 | **完整 Git 仓**（含提交历史）。见 D-15 |
| Q16 | ⏳ 开放<br>（评审前需定） | 各角色评审负责人与评审时间窗 | 需求文档与设计文档的评审排期 |
| Q17 | ⏳ 开放<br>（M0 内需定） | `clipanion` 目前最新为预发布版 `4.0.0-rc.4` | 锁 rc 版 vs 降到稳定的 3.x。功能已验证可用，属版本策略选择（M0 决定） |
| Q18 | ⏳ 开放<br>（M5 前需定） | 布局在**浏览器侧**还是**服务端**执行 | 浏览器侧：`elkjs` 默认路径直接可用（`self`/`document` 存在）；服务端：需按运行时显式传 `workerFactory`（见 §12.1 布局算法行） |

---

## 附录 A：设计准则速查

1. **代码能证明的，绝不给 LLM** —— 三级判定：字面证明 → 规则推导 → 语义判断
2. **LLM 只提议，不落库** —— 所有候选过四道闸门才进事实源
3. **证据必须回验** —— 路径存在、行号命中、片段一致，三者同时为真
4. **ID 内容寻址** —— 禁行号、禁自增、禁 UUID、禁绝对路径
5. **缓存键含依赖契约面** —— 否则后端改签名、前端命中缓存，静默漏报
6. **拿不准就升级全量** —— 宁可慢一次，不要错一版
7. **范围守卫单点收口** —— 阶段代码物理上拿不到 `fs`
8. **写不出 fallback 的任务不该给 LLM** —— 编译期强制
9. **变更即确认失效** —— 模块重跑后状态回退 `pending`
10. **任何未落盘的知识都视为已丢失** —— 以模块为检查点，原子写盘
11. **路径基准永远是系统根** —— 不是子项目根，更不是子仓根；否则同名文件互相覆盖、内容寻址失效
12. **技术验证必须设对照组** —— 只在目标运行时上跑，会把「依赖陈旧」误记为「运行时缺陷」；本次正是靠 Node 对照避免了一次误判（§12.2.1）
12. **不引入 LLM 编排框架** —— 需要的那点编排自己写，规模可控，且每一行都在能被控制的位置
13. **密钥与产物物理隔离** —— 凭据只留在进程环境或系统根之外，绝不进入可评审、可分发、可打包的文本产物



